<% layout('layouts/boilerplate')%>
    <h1>
        All Campgrounds
    </h1>
    <div>
        <button><a href="/campgrounds/new">Add Campground</a></button>
    </div>
    <% for(let c of campgrounds) { %>
            <div class="card mb-3">
                <div class="row">
                    <div class="col-md-4">
                        <% if(c.images.length) { %> 
                        <img class="img-fluid" alt="" src="<%=c.images[0].url%>">
                        <% } else { %>
                            <img class="img-fluid" alt="" src="https://res.cloudinary.com/dc2wtilcq/image/upload/v1616631506/YelpCamp/biijmookjrog4gfxqcwl.jpg">
                        <% } %> 
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%=c.title%></h5>
                            <p class="card-text"><%= c.description%> </p>
                            <p class="card-text">
                                <small class="text-muted"><%= c.location %></small>
                            </p>
                            <a class="btn btn-primary" href="../campgrounds/<%=c._id%>">See more</a>
                        </div>
                    </div>
                </div>
            </div>
    <% } %>