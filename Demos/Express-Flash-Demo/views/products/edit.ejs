<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit Product</h1>
    <form action="/products/<%=product._id%>?_method=PUT" method="post">
        <label for="name">Product Name</label>
        <input type="text" id="name" name="name" placeholder="product name" value="<%=product.name%>">
        <br>
        <label for="price">Unit Price</label>
        <input type="numer" name="price" id="price" placeholder="product price" value="<%=product.price%>">
        <br>
        <label for="category">Select Category</label>
        <select name="category" id="category">
            <% for(let cat of categories) { %> 
                <option value="<%=cat%>" <%= product.category === cat? 'selected':'' %> ><%=cat%> </option>
            <% } %> 
        </select>
        <br>
        <button>Done</button>
    </form>
    <button><a href="/products/<%=product._id%>">Cancel</a></button>
</body>

</html>